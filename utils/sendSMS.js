// /utils/sendSMS.js
const sendSMS = async (phoneNumber, message) => {
    console.log(`SMS sent to ${phoneNumber}: ${message}`);
    // Replace this with real integration e.g. Twilio, Fast2SMS, etc.
};

module.exports = sendSMS;